#include <stdio.h>
/**
 * main - print alphabet
 * description: use lowercase followed by newline
 * return: (0)
*/
int main(void)
{
int ch;
for (ch = 97; ch <= 122; ch = ++)
{
putchar(char);
}
putchar(10);
return (0);
}
